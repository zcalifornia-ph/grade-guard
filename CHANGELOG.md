# Changelog

Status: `1.0.0` is the current supported release line for the Grade Guard Windows console application.

## v1.0.0 - 2026-03-14

### Added or Changed
- Bumped the repository release metadata from `v0.1.14` to `v1.0.0`.
- Rewrote `README.md` so it describes the application, repository layout, usage flow, and validation process without the older milestone-tracking language.
- Updated `scripts/build.ps1` and `scripts/validate.ps1` to use release-facing artifact directories by default.
- Updated `CONTRIBUTING.md` and `SECURITY.md` for the `1.0.x` line.
- Added `docs/version-1-0-0-docs.md` with release notes for the 1.0.0 pull request baseline.
- Verified `powershell -ExecutionPolicy Bypass -File .\scripts\validate.ps1` still passes with the new documentation and artifact paths.

### For Deletion
- `artifacts/unit-6-bolt-6-1/` (older prerelease validation output directory still present in the workspace).
- `artifacts/unit-6-bolt-6-2/` (older prerelease validation output directory still present in the workspace).
- `artifacts/validation/` (current validation output directory generated by the updated script).
- `grade-guard.exe` (root-level local binary artifact present in the workspace).
- `0.csv` (numbered profile output currently present in the working directory).
- `grade-guard/bolt-4-1-main.exe`, `grade-guard/bolt-4-2-main.exe`, `grade-guard/bolt-5-1-main.exe`, and `grade-guard/bolt-5-2-main.exe` (older acceptance-build artifacts still present in the workspace).
- `grade-guard/tests/vector_test.exe` (local vector regression-test binary present in the workspace).
- `grade-guard/unit-tests/models_lifecycle_test.exe`, `grade-guard/unit-tests/persistence_contract_test.exe`, and `grade-guard/unit-tests/unit5_defect_baseline_test.exe` (local regression-test binaries present in the workspace).
- `grade-guard/unit-tests/persistence-blank-lines.csv`, `grade-guard/unit-tests/persistence-invalid-number.csv`, `grade-guard/unit-tests/persistence-legacy-fixture.csv`, `grade-guard/unit-tests/persistence-missing-field.csv`, `grade-guard/unit-tests/persistence-oversized-record.csv`, `grade-guard/unit-tests/persistence-shape-lecture.csv`, `grade-guard/unit-tests/persistence-shape-lab.csv`, and `grade-guard/unit-tests/persistence-shape-mixed.csv` (generated persistence-regression fixture outputs).
- `grade-guard/unit-tests/persistence-runtime/` (generated numbered runtime-output directory currently containing local profile outputs).
- `grade-guard/unit-tests/app-smoke-runtime/` (generated app-smoke runtime directory containing local profile outputs).

## v0.1.14 - 2026-03-14

### Added or Changed
- Published the final prerelease documentation pass for the modular Windows console codebase.
- Kept the full build-and-validation workflow documented and reproducible through the PowerShell automation under `scripts/`.

### For Deletion
- Local validation artifacts and generated runtime outputs created during prerelease verification.

## v0.1.x Series - 2026-03-12 to 2026-03-14

### Added or Changed
- Split the original monolithic prototype into focused modules for app flow, profile management, vectors, models, grade calculation, persistence, and console UI helpers.
- Added regression coverage for vector behavior, lifecycle handling, CSV persistence, grading edge cases, and application smoke flows.
- Hardened local CSV persistence, preserved compatibility for older saved profiles, and standardized the contributor build/validation workflow.
- Added detailed release notes under `docs/` for each prerelease milestone.

### For Deletion
- Generated local binaries, validation outputs, and numbered profile CSV files produced during development and testing.

## v0.1.0 - 2026-03-12

### Added or Changed
- Imported the first Grade Guard Windows console source tree and project-facing root documentation.

### For Deletion
- Generated local binaries and runtime profile data files.

## v0.0.1 - 2026-03-12

### Added or Changed
- Established the initial repository documentation baseline for Grade Guard.

### For Deletion
- None from the initial documentation setup.
